pub use self::data::defaults;

pub mod data {
    pub mod defaults {
        pub mod name;
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        // let result = add(2, 2);
        // assert_eq!(result, 4);
    }
}
